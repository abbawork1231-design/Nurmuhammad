rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /expert-photos/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.token.email == "abbawork1231@gmail.com";
    }
  }
}
